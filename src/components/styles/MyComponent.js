import React from 'react';
import styles from './MyComponent.module.css'; // Import styling module

const MyComponent = () => {
  return <div className={styles.myComponent}>Styled Component</div>; // Use the class from the styling module
};

export default MyComponent;
